[project]
name = "discount_analyst"
version = "0.1.0"
description = "An AI-powered stock analysis tool. It was named 'Discount Analyst' because it is intended to be a cheap stock analysis tool."
authors = [
    {name = "Samuel Harris",email = "samuel.matsuo.harris@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pydantic (>=2.12.3,<3.0.0)",
    "yfinance (>=0.2.66,<0.3.0)",
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=8.4.2,<9.0.0)",
    "pytest-cov (>=7.0.0,<8.0.0)",
    "ruff (>=0.14.2,<0.15.0)",
    "pre-commit (>=4.3.0,<5.0.0)",
    "pyrefly (>=0.42.2,<0.43.0)",
    "pandas-stubs (>=2.3.2.250926,<3.0.0.0)",
]

[tool.pyrefly]
python-version = "3.14.0"
project-includes = [
    "discount_analyst/data_fetcher",
    "discount_analyst/dcf_analysis"
]
infer-with-first-use = false
untyped-def-behavior = "check-and-infer-return-type"
